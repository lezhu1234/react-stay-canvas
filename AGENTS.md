# 本仓库工程质量要求

## 适用范围

本文件仅适用于 `react-stay-canvas` 仓库及其子目录，不代表用户其他仓库的默认规则。

## 硬性质量门槛

- 架构整洁、职责清晰、逻辑优雅是交付的硬性要求，不是可选优化。
- 测试、类型检查和构建通过只是必要条件；行为正确但结构混乱的代码仍不得交付。
- 现有代码中的坏结构、深层嵌套、职责混合或命名问题不能作为新增代码的风格依据。不得以“保持旧代码一致”为由继续复制坏模式。
- Code Review 必须同时审查正确性和可维护性。新增或修改代码若明显增加嵌套、分支耦合、隐式状态或职责混合，应视为阻塞交付的问题。

## 实现要求

- 开始实现前，先明确核心流程、状态与所有权、不变量、失败与终止路径，以及每个模块应承担的职责。
- 一个函数只处理一个抽象层级。编排函数负责串联步骤，具体判断、状态转换、目标解析和副作用应由命名明确的函数承载。
- 优先使用 guard clause、早返回和具名决策函数，避免不断叠加 `if/else`、多层嵌套和难以解释的布尔条件组合。
- 当一个分支代表稳定的领域概念时，应为该概念命名并建立清晰边界，而不是继续把特殊情况塞进原函数。
- 状态只能有清晰的单一所有者；开始、继续、正常结束、取消和销毁必须具有明确且统一的生命周期出口，清理逻辑不得散落在多个分支中。
- DOM 输入、指针会话、事件构造、目标解析、监听器筛选和用户回调派发应保持职责分离，不得在一个大函数里交叉处理。
- 优先提取小而明确的私有函数。只有在确实存在独立状态和生命周期所有权时才新增类或抽象层，禁止为了形式整洁进行无依据的过度设计。
- 注释解释不变量、设计原因和非显然约束，不复述代码表面行为，也不依赖长注释掩盖复杂控制流。
- 修改到结构较差的旧代码时，不要求顺手重写整个仓库；但必须重构完成当前功能所需的最小边界，不能沿着旧结构继续堆补丁。

## 验证要求

- 测试应覆盖公开行为、核心状态转换、终止与清理路径，以及本次重构必须保持的不变量。
- 重构不能只依赖“现有测试仍然通过”；还应检查主流程是否能自上而下阅读、分支是否互斥明确、状态是否有唯一来源、异常与清理是否拥有统一出口。
- CR 发现“功能正确但结构难以安全修改”的代码时，不得以测试通过为由判定 clean。
